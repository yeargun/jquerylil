export JsValue siblings(JsValue start, JsValue elem = null) {
  JsValue[] matched = [];
  for (JsValue n = start; n.truthy(); n = n["nextSibling"]) {
    if (n["nodeType"] == 1.0 && n != elem) {
      matched.push(n);
    }
  }
  return matched;
}
