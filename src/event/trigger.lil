// jQuery.event.trigger/simulate and $.fn.trigger/triggerHandler live in ../event.lil, next to
// the dispatch machinery they drive; this module keeps the upstream module name for importers.
import { jQuery, trigger } from "../event";

export { jQuery, trigger };
