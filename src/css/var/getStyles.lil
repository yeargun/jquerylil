import { windowSelf } from "../../js-host.lil";

export JsValue getStyles(JsValue elem) {
  JsValue view = elem["ownerDocument"]["defaultView"];
  if (!view.truthy() || !view["opener"].truthy()) {
    view = windowSelf();
  }
  return JS.invoke(view, "getComputedStyle", elem);
}
