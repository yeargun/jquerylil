import { jQuery } from "../core";
import { scheduleTimeout } from "../js-host.lil";

jQuery["readyException"] = (JsValue error) => {
  scheduleTimeout(() => {
    throw error;
  });
};

export { jQuery };
