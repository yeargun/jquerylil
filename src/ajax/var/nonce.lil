import {
  dateNow
} from "../../js-host.lil";

// Upstream's `nonce` is a private `{ guid: Date.now() }` bag that only ajax.js
// and jsonp.js read, always as `nonce.guid++`; a counter says the same thing.
float guid = dateNow();

export float nextGuid() {
  return guid++;
}
