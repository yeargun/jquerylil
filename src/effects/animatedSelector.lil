import { jQuery, grep } from "../core";
import { expr } from "../selector";

expr["pseudos"]["animated"] = (JsValue elem) => grep(jQuery["timers"], (JsValue fn) => elem == fn["elem"]).length;

export { jQuery };
